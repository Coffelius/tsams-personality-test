name = "sams-personality-test"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# Workers AI binding - set in Cloudflare dashboard or via:
# wrangler pages secret put AI_BINDING --project-name=sams-personality-test

[vars]
ENVIRONMENT = "production"
